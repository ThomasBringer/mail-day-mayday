using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RealVelocity : MonoBehaviour
{
    // Rigidbody2D rb;
    // Vector2 previousPos;

    // void Awake()
    // {
    //     rb = GetComponent<Rigidbody2D>();
    //     previousPos = rb.position;
    // }

    // void FixedUpdate()
    // {
    //     offset = rb.position - previousPos;
    //     previousPos = rb.position;
    // }

    // [HideInInspector] public Vector2 offset;
    // public bool HasMoved => offset != Vector2.zero;

    // public bool HasMoved => offset.sqrMagnitude > 1f;
}
